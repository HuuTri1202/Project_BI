# =============================================================================
# Frontend (Vite) — copy file này thành .env.
#
# CẢNH BÁO: mọi biến VITE_* đều được NHÚNG THẲNG vào bundle và người dùng cuối
# đọc được. Tuyệt đối không đặt secret ở đây (API token của Strapi, khoá S3,
# secret ký JWT...).
# =============================================================================

# Gốc của API. Để nguyên đường dẫn tương đối để đi qua proxy của Vite khi dev.
# Phải là /api (KHÔNG phải /api/v1): router xác thực mount ở /api/auth, còn
# /api/v1 chỉ là một nhánh con — đặt sai thì mọi lời gọi đăng nhập ăn 404.
VITE_API_BASE_URL=/api

# Backend mà dev server proxy tới (chỉ dùng lúc dev, không vào bundle)
VITE_API_PROXY_TARGET=http://localhost:4000
